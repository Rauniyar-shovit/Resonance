export const TEXT_MAX_LENGTH = 5000;
export const COST_PER_UNIT = 0.0005;

/**
 * Flat charge per voice generated, in dollars.
 *
 * Lives beside the speech rate so the landing page and the workspace quote the same
 * numbers. Both must match the Polar meters.
 */
export const VOICE_GENERATION_COST = 0.4;
