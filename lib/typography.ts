/**
 * Mono, wide-tracked, uppercase — the label voice used across the workspace.
 *
 * Section labels, meters, and rates all speak in it, so the exact type settings live
 * here rather than being re-typed per panel and drifting apart.
 */
export const EYEBROW =
  "font-mono text-[11px] uppercase tracking-[0.14em] text-muted-foreground";
